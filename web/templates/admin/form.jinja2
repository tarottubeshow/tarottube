{% macro text(
    name,
    value,
    label,
    type="text",
    readonly=False
) %}
    <div class="form-group">
        <label for="{{ name }}">{{ label }}</label>
        <input
            class="form-control"
            placeholder="{{ label }}"
            type="{{ type }}"
            name="{{ name }}"
            id="{{ name }}"
            value="{{ value or '' }}"
            {% if readonly %}readonly{% endif %}
        />
        <i class="form-group__bar"></i>
    </div>
{% endmacro %}
